[build]
  command = "npm run build"
  publish = ".next"
  environment = { NODE_VERSION = "18.17.0" }

# This tells Netlify to use Next.js runtime
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/index"
  status = 200

[dev]
  command = "npm run dev"
  port = 3000
  framework = "next"

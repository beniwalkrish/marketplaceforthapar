'use client';import { useState } from 'react';import { Mail, Lock, User, BookOpen } from 'lucide-react';import { useRouter } from 'next/navigation';import { toast } from 'sonner';import clsx from 'clsx';import { signUpUser, signInUser, createUserProfile } from '@/lib/supabase';export default function AuthPage() {  const router = useRouter();  const [isSignUp, setIsSignUp] = useState(false);  const [isLoading, setIsLoading] = useState(false);  const [formData, setFormData] = useState({    email: '',    password: '',    name: '',    confirmPassword: '',    branch: '',    year: '',  });  const handleInputChange = (e: React.ChangeEvent<HTMLInputElement | HTMLSelectElement>) => {    const { name, value } = e.target;    setFormData((prev) => ({ ...prev, [name]: value }));  };  const validateThaparEmail = (email: string): boolean => {    return email.toLowerCase().endsWith('@thapar.edu');  };  const handleSubmit = async (e: React.FormEvent) => {    e.preventDefault();    if (!validateThaparEmail(formData.email)) {      toast.error('Only Thapar students can join CampusBazaar. Please use your @thapar.edu email address.');      return;    }    if (isSignUp && formData.password !== formData.confirmPassword) {      toast.error('Passwords do not match');      return;    }    if (isSignUp && !formData.name) {      toast.error('Please enter your full name');      return;    }    if (isSignUp && (!formData.branch || !formData.year)) {      toast.error('Please select your branch and year');      return;    }    setIsLoading(true);    try {      if (isSignUp) {        // Sign up new user        const { user } = await signUpUser(formData.email, formData.password, formData.name);                // Create user profile in users table        if (user) {          await createUserProfile(user.id, {            email: formData.email,            name: formData.name,            branch: formData.branch,            year: parseInt(formData.year),            is_verified: false,            rating: 0,          });        }                toast.success('Account created successfully! Check your email to verify.');        setFormData({ email: '', password: '', name: '', confirmPassword: '', branch: '', year: '' });        setIsSignUp(false);      } else {        // Sign in user        await signInUser(formData.email, formData.password);        toast.success('Logged in successfully!');        router.push('/');      }    } catch (error: any) {      toast.error(error.message || 'Authentication failed');    } finally {      setIsLoading(false);    }  };  return (    <div className="min-h-screen bg-gradient-to-br from-primary-900 via-slate-900 to-primary-900 flex items-center justify-center px-4 py-8">      <div className="w-full max-w-md">        <div className="bg-white rounded-3xl p-8 shadow-2xl">          {/* Logo */}          <div className="flex items-center justify-center gap-2 mb-8">            <div className="w-10 h-10 bg-gradient-to-br from-accent to-orange-600 rounded-lg flex items-center justify-center text-white font-bold text-lg">              CB            </div>            <span className="text-2xl font-heading font-bold text-primary-900">CampusBazaar</span>          </div>          <h1 className="text-3xl font-heading font-bold text-primary-900 text-center mb-2">            {isSignUp ? 'Join CampusBazaar' : 'Welcome Back'}          </h1>          <p className="text-center text-slate-600 mb-8">            {isSignUp              ? 'Create your account with your Thapar email'              : 'Sign in to your CampusBazaar account'}          </p>          <form onSubmit={handleSubmit} className="space-y-4">            {/* Sign Up Fields */}            {isSignUp && (              <>                <div>                  <label className="block text-sm font-heading font-semibold text-primary-900 mb-2">                    Full Name                  </label>                  <div className="relative">                    <User className="absolute left-4 top-3.5 w-5 h-5 text-slate-400" />                    <input                      type="text"                      name="name"                      value={formData.name}                      onChange={handleInputChange}                      placeholder="John Doe"                      required                      className="w-full pl-12 px-4 py-3 border-2 border-slate-200 rounded-lg focus:outline-none focus:border-accent"                    />                  </div>                </div>                <div>                  <label className="block text-sm font-heading font-semibold text-primary-900 mb-2">                    Branch                  </label>                  <select                    name="branch"                    value={formData.branch}                    onChange={handleInputChange}                    required                    className="w-full px-4 py-3 border-2 border-slate-200 rounded-lg focus:outline-none focus:border-accent"                  >                    <option value="">Select your branch</option>                    <option value="CSE">Computer Science</option>                    <option value="ECE">Electronics & Communication</option>                    <option value="EE">Electrical Engineering</option>                    <option value="ME">Mechanical Engineering</option>                    <option value="BT">Biotechnology</option>                    <option value="CE">Civil Engineering</option>                  </select>                </div>                <div>                  <label className="block text-sm font-heading font-semibold text-primary-900 mb-2">                    Year                  </label>                  <select                    name="year"                    value={formData.year}                    onChange={handleInputChange}                    required                    className="w-full px-4 py-3 border-2 border-slate-200 rounded-lg focus:outline-none focus:border-accent"                  >                    <option value="">Select your year</option>                    <option value="1">1st Year</option>                    <option value="2">2nd Year</option>                    <option value="3">3rd Year</option>                    <option value="4">4th Year</option>                  </select>                </div>              </>            )}            {/* Email */}            <div>              <label className="block text-sm font-heading font-semibold text-primary-900 mb-2">                Email Address              </label>              <div className="relative">                <Mail className="absolute left-4 top-3.5 w-5 h-5 text-slate-400" />                <input                  type="email"                  name="email"                  value={formData.email}                  onChange={handleInputChange}                  placeholder="you@thapar.edu"                  required                  className="w-full pl-12 px-4 py-3 border-2 border-slate-200 rounded-lg focus:outline-none focus:border-accent"                />              </div>              <p className="text-xs text-slate-500 mt-1 flex items-center gap-1">                <BookOpen className="w-3 h-3" />                Only @thapar.edu email addresses are allowed              </p>            </div>            {/* Password */}            <div>              <label className="block text-sm font-heading font-semibold text-primary-900 mb-2">                Password              </label>              <div className="relative">                <Lock className="absolute left-4 top-3.5 w-5 h-5 text-slate-400" />                <input                  type="password"                  name="password"                  value={formData.password}                  onChange={handleInputChange}                  placeholder="••••••••"                  required                  className="w-full pl-12 px-4 py-3 border-2 border-slate-200 rounded-lg focus:outline-none focus:border-accent"                />              </div>            </div>            {/* Confirm Password */}            {isSignUp && (              <div>                <label className="block text-sm font-heading font-semibold text-primary-900 mb-2">                  Confirm Password                </label>                <div className="relative">                  <Lock className="absolute left-4 top-3.5 w-5 h-5 text-slate-400" />                  <input                    type="password"                    name="confirmPassword"                    value={formData.confirmPassword}                    onChange={handleInputChange}                    placeholder="••••••••"                    required                    className="w-full pl-12 px-4 py-3 border-2 border-slate-200 rounded-lg focus:outline-none focus:border-accent"                  />                </div>              </div>            )}            {/* Submit Button */}            <button              type="submit"              disabled={isLoading}              className={clsx(                'w-full py-3 rounded-lg font-heading font-bold text-lg transition-all mt-6',                isLoading                  ? 'bg-slate-300 text-slate-500 cursor-not-allowed'                  : 'bg-accent text-white hover:bg-orange-500'              )}            >              {isLoading ? 'Loading...' : isSignUp ? 'Create Account' : 'Sign In'}            </button>            {/* Toggle Sign Up/In */}            <div className="text-center mt-6">              <p className="text-slate-600 text-sm">                {isSignUp ? 'Already have an account?' : "Don't have an account?"}                <button                  type="button"                  onClick={() => {                    setIsSignUp(!isSignUp);                    setFormData({ email: '', password: '', name: '', confirmPassword: '', branch: '', year: '' });                  }}                  className="text-accent font-heading font-bold hover:text-orange-500 ml-2"                >                  {isSignUp ? 'Sign In' : 'Sign Up'}                </button>              </p>            </div>          </form>          {/* Info Box */}          <div className="bg-blue-50 border border-blue-200 rounded-lg p-4 mt-8 text-sm text-blue-800">            <p className="font-heading font-semibold mb-1">Only Thapar Students</p>            <p>CampusBazaar is exclusively for Thapar Institute of Engineering & Technology students. Sign up with your official @thapar.edu email address.</p>          </div>        </div>      </div>    </div>  );}